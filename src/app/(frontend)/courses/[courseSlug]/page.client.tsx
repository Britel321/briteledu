'use client'

import { useEffect } from 'react'

export default function PageClient() {
  useEffect(() => {
    // Any client-side logic for the course detail page
    // For example, analytics tracking, scroll effects, enrollment tracking, etc.
  }, [])

  return null
}
